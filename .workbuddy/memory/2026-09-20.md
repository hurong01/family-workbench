# 2026-09-20

## 家庭账本小程序上线评审（全量静态审计）

对 `E:\08-VX\family-workbench`（AppID `wx5fd5bd5259d01a00`）做了完整代码审计：8 个自研页面、13 个云函数、3 份设计文档。

### 已实现功能
家庭创建 / 邀请码加入（6 位、7 天有效、可分享）/ 家庭工作台（月支出、预算进度、待缴、快捷记账、最近流水）/ 记一笔（9 分类、付款人、票据上传）/ 月度账单（按月筛选、分类占比、编辑删除）/ 成员管理（转移管理员、撤销邀请、删除家庭）/ 固定费用提醒 / 导出 CSV + 云端备份恢复 / 隐私说明页。

### 安全结论（重要）
**未发现越权、注入、跨家庭数据泄露问题。** 云函数鉴权扎实：所有读写走云函数、每函数校验 OpenID 与 familyId 归属、改删有「管理员或本人」双条件、转移管理员与删除家庭用事务、导出做了 CSV 公式注入转义。这是本项目最大加分项。

### 缺陷清单（按优先级）
**P0（上线必卡）**
1. 收集 OpenID + 相册/摄像头，但**未在公众平台配置《用户隐私保护指引》** —— 2023 起硬性驳回项。
2. `pages/expense-add/index.js` 票据上传失败只弹 toast，`receiptTempPath` 仍显示本地图，用户误以为成功 → **静默丢图**。
3. `pages/expense-add/index.js` 编辑态下 `members.indexOf(payerName)` 时机/显示不同步 → **付款成员可能被静默改成第一个人**。

**P1**
4. `profile/index.js` 未处理登录失败，`r.family.name` 可能抛错白屏。
5. 多条邀请码叠加时分享的是最新那条，旧码分享出去失效。
6. `manageFamily delete` 超 150 项报错，文案让用户找管理员，无自助方案。
7. `restoreFamily` 恢复后 `payerName` 可能指向已改名/不存在的成员。

**P2 代码卫生**
- 残留整套云开发模板：`pages/index/`、`pages/example/`、`components/cloudTipModal/`、`images/` 下 26 张演示图（约 1.3MB）；`index.js` 硬编码调用不存在的 `quickstartFunctions`。建议删除。
- `README.md` 仍是「云开发 quickstart」原文；`uploadCloudFunction.sh` 是未替换的占位脚本。
- `app.js` 硬编码环境 ID `cloudfunctions-d1gi62wijfa055937`，未用 `envList.js`。
- `sitemap.json` 允许全部页面被索引，但这是私密账本，应改 `disallow`。
- `project.private.config.json` 未被 `.gitignore` 忽略，已进仓库。
- 页面 WXSS 颜色硬编码重复，未抽 CSS 变量。

### 发布前结论
技术上跑得通，但**当前状态会被审核驳回**。修完 3 个 P0 + 隐私声明后具备提审条件。提审必须提供**已创建家庭的测试账号**，否则审核员只看到 onboarding 页，会以「功能不完整」驳回。

### 待办（用户尚未确认是否动手）
按 P0-2/P0-3 → 模板残留清理 → P1 → 隐私文案对齐的顺序修复。

const cloud=require('wx-server-sdk');cloud.init({env:cloud.DYNAMIC_CURRENT_ENV});const db=cloud.database(),categories=['房租','水费','电费','宽带费','物业费','生活用品','燃气费','维修费','其他'];
exports.main=async e=>{try{const{OPENID}=cloud.getWXContext(),amount=Number(e.amount),paidAt=String(e.paidAt||''),category=String(e.category||'');if(!Number.isFinite(amount)||amount<=0||amount>10000000)return{success:false,message:'金额不正确'};if(!categories.includes(category)||!/^\d{4}-\d{2}-\d{2}$/.test(paidAt))return{success:false,message:'分类或日期不正确'};const users=await db.collection('users').where({_openid:OPENID}).limit(1).get(),user=users.data[0];if(!user)return{success:false,code:'NO_FAMILY',message:'尚未加入家庭'};const family=(await db.collection('families').doc(user.familyId).get()).data,payerName=String(e.payerName||'');if(!(family.members||[]).some(x=>x.name===payerName))return{success:false,message:'付款成员不属于当前家庭'};const result=await db.collection('expenses').add({data:{familyId:user.familyId,creatorOpenid:OPENID,amount,category,payerName,paidAt,month:paidAt.slice(0,7),note:String(e.note||'').trim().slice(0,100),createdAt:db.serverDate(),updatedAt:db.serverDate()}});return{success:true,expenseId:result._id}}catch(e){console.error(e);return{success:false,message:'保存账目失败'}}}
